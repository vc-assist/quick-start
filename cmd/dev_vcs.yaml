procs:
  frontend:
    cwd: "frontend"
    shell: "pnpm dev"
  desktop:
    cwd: "desktop"
    shell: "pnpm start --dev"
  backend_auth:
    cwd: "backend/cmd/auth"
    shell: "go run ."
  backend_vcs:
    cwd: "backend/cmd/vcs"
    shell: "go run ."
