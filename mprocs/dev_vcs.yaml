procs:
  frontend:
    cwd: "frontend"
    shell: "pnpm dev"
  desktop:
    cwd: "desktop"
    shell: "pnpm dev"
  backend:
    cwd: "backend/cmd/vc-server"
    shell: "go run . -v"
  docker_services:
    cwd: "backend"
    shell: "go run ./dev"
