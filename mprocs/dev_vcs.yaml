procs:
  frontend:
    cwd: "frontend"
    shell: "pnpm dev"
  desktop:
    cwd: "desktop"
    shell: "pnpm start --dev"
  backend:
    cwd: "backend/cmd/vc-server"
    shell: "go run . -v"
  backend_devenv:
    cwd: "backend"
    shell: "go run ./dev"
